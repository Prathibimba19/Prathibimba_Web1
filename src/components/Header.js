//This will be our Header which will always be tucked to the top in the website

const Header = () =>
{
    return(
        <div>
            This is Header
        </div>
    )
}

export default Header;